;;; init-window.el --- Defaults for window -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(setq display-line-numbers-type 'relative)
(global-display-line-numbers-mode)

(provide 'init-window)
;;; init-window.el ends here
